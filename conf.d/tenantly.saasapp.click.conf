server {
    listen      80;
    listen      [::]:80;
    server_name tenantly.saasapp.click;

    # reverse proxy
    location / {
        proxy_set_header Host $host;
        proxy_pass            http://localhost:3000;
        # include               /etc/nginx/conf.d/nginxconfig.io/proxy.conf;
    }
}